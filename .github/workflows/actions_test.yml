name: Actions Test

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Say hello
        run: echo "hello from Actions Test"
